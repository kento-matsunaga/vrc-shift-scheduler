# Public Alpha Release タスクリスト（VRC Shift Scheduler）

## 🎯 このタスクファイルのゴール

**「一般人テスター」が実際に触って動作確認できる状態にする**

テスターができることの定義：
1. ✅ ブラウザだけでアクセス（ログイン/識別ができる）
2. ✅ 自分が属する「イベント」を選ぶ or 作る
3. ✅ 営業日を作る
4. ✅ 営業日の中にシフト枠を作る
5. ✅ メンバーを登録して、そのメンバーにシフトを割り当てる
6. ✅ 「自分のシフト一覧」を見られる
7. ✅ テナントごとにデータが混ざらない
8. ✅ サーバーはクラウド等で常時起動
9. ✅ 致命的に落ちても DB をリセットしてやり直せる
10. ✅ 使い方の簡単な説明と、バグ報告の経路がある

**このタスクファイルは「Public Alpha Test 版」のゴールに特化しています。**

---

## タスクステータス・優先度マーカー

### ステータス
- `[ ]` To Do
- `[~]` In Progress（同時に1つだけ）
- `[x]` Done
- `[!]` Blocked

### 優先度
- 🔴 **最優先**（これがないとテスト開始できない）
- 🟡 **次点**（テストしながら整える）
- 🟢 **余裕があれば**（テスト後でもOK）

---

## 📐 実装順序の戦略

### ステップ1: バックエンド API 完成（フェーズA） 🥇
**現状**: Event/BusinessDay API は完成済み  
**残り**: ShiftSlot/Assignment/Member API の実装

### ステップ2: フロントエンド最小実装（フェーズB） 🥈
**ゴール**: テスターが「ブラウザだけで一通りの操作」ができる UI

### ステップ3: ステージング環境構築（フェーズC） 🥉
**ゴール**: 常時稼働するテスト環境 + データリセット手段

### ステップ4: ドキュメント整備（フェーズD） 🏅
**ゴール**: Quick Start + バグ報告フロー

---

## フェーズA: バックエンド機能の穴埋め（APIだけで一応完結させる）

### 🔴 **親タスク A-1: ShiftAssignment Application Service の実装**

シフト確定のビジネスロジックを Application Service として実装する。  
**依存**: 既存の ShiftAssignment/ShiftSlot/Member リポジトリ

- [x] 🔴 **A-1.1: ShiftAssignmentService の実装** [最優先] ✅ **完了 (2025-12-03)**
  - *詳細*: `backend/internal/app/shift_assignment_service.go`
  - *メソッド*:
    - `ConfirmManualAssignment(ctx, tenantID, slotID, memberID, actorID, note) (*ShiftAssignment, error)`
  - *ロジック*:
    1. ShiftSlot を取得（tenant_id チェック）
    2. `SELECT ... FOR UPDATE` で該当 slot の assignments をロック
    3. 既存の confirmed 件数をカウント
    4. `count >= required_count` なら `ErrSlotFull` を返す
    5. ShiftAssignment を作成・保存
    6. Notification stub 呼び出し（`logger.Info("シフト確定通知: ...")`）
    7. AuditLog stub 記録（`logger.Info("AuditLog: actor_id=%s assigned member_id=%s to slot_id=%s", ...)`）
  - *見積もり*: 3〜4時間
  - *テスト*: 同時確定の統合テスト（goroutine で複数リクエスト）

- [ ] 🔴 **A-1.2: ShiftAssignmentService の統合テスト** [最優先]
  - *シナリオ*:
    - 正常系: 枠に空きがあるとき確定成功
    - 異常系: required_count を超えたら 409
    - 同時実行: 5人が3枠に同時確定 → 3人だけ成功、2人は失敗
  - *詳細*: `backend/internal/app/shift_assignment_service_test.go`
  - *見積もり*: 2〜3時間

---

### 🔴 **親タスク A-2: ShiftSlot 管理 API の実装**

営業日に対してシフト枠を作成・取得する REST API。

- [x] 🔴 **A-2.1: POST /api/v1/business-days/:business_day_id/shift-slots** [最優先] ✅ **完了 (2025-12-03)**
  - *リクエスト*:
    ```json
    {
      "position_id": "01HXXXX...",
      "slot_name": "受付",
      "instance_name": "受付1",
      "start_time": "21:30",
      "end_time": "23:00",
      "required_count": 2,
      "priority": 1
    }
    ```
  - *バリデーション*:
    - start_time < end_time（または overnight 判定）
    - required_count >= 1
    - business_day_id が tenant 内に存在
  - *レスポンス*:
    ```json
    {
      "data": {
        "slot_id": "01HXXXX...",
        "tenant_id": "01HXXXX...",
        "business_day_id": "01HXXXX...",
        "position_id": "01HXXXX...",
        "slot_name": "受付",
        "instance_name": "受付1",
        "start_time": "21:30:00",
        "end_time": "23:00:00",
        "required_count": 2,
        "priority": 1,
        "is_overnight": false,
        "created_at": "2025-12-03T12:00:00+09:00",
        "updated_at": "2025-12-03T12:00:00+09:00"
      }
    }
    ```
  - *詳細*: `backend/internal/interface/rest/shift_slot_handler.go`
  - *見積もり*: 2〜3時間

- [x] 🔴 **A-2.2: GET /api/v1/business-days/:business_day_id/shift-slots** [最優先] ✅ **完了 (2025-12-03)**
  - *レスポンス*:
    ```json
    {
      "data": {
        "shift_slots": [
          {
            "slot_id": "...",
            "slot_name": "受付",
            "instance_name": "受付1",
            "start_time": "21:30:00",
            "end_time": "23:00:00",
            "required_count": 2,
            "assigned_count": 1,
            "status": "需要募集中"
          }
        ],
        "count": 3
      }
    }
    ```
  - *補足*: `assigned_count` は ShiftAssignment テーブルを JOIN して算出
  - *見積もり*: 2時間

- [x] 🔴 **A-2.3: GET /api/v1/shift-slots/:slot_id** [最優先] ✅ **完了 (2025-12-03)**
  - *レスポンス*: ShiftSlot情報 + 確定済みメンバー一覧 + 営業日情報
  - *見積もり*: 1〜2時間

---

### 🔴 **親タスク A-3: ShiftAssignment 管理 API の実装**

シフト確定・取得の REST API。

- [x] 🔴 **A-3.1: POST /api/v1/shift-assignments** [最優先] ✅ **完了 (2025-12-03)**
  - *リクエスト*:
    ```json
    {
      "slot_id": "01HXXXX...",
      "member_id": "01HXXXX...",
      "note": "急遽対応"
    }
    ```
  - *処理*: **Application Service 経由必須**
    - `ShiftAssignmentService.ConfirmManualAssignment()` を呼ぶ
    - ハンドラから直接 Repository を触らない
  - *レスポンス*:
    ```json
    {
      "data": {
        "assignment_id": "01HXXXX...",
        "slot_id": "01HXXXX...",
        "member_id": "01HXXXX...",
        "assignment_status": "confirmed",
        "assignment_method": "manual",
        "assigned_at": "2025-12-03T12:00:00+09:00",
        "notification_sent": false
      }
    }
    ```
  - *エラーレスポンス例*:
    ```json
    {
      "error": {
        "code": "ERR_SLOT_FULL",
        "message": "このシフト枠は既に満員です",
        "details": {
          "slot_id": "01HXXXX...",
          "required_count": 2,
          "current_count": 2
        }
      }
    }
    ```
  - *詳細*: `backend/internal/interface/rest/shift_assignment_handler.go`
  - *見積もり*: 2〜3時間

- [x] 🔴 **A-3.2: GET /api/v1/shift-assignments** [最優先] ✅ **完了 (2025-12-03)**
  - *クエリパラメータ*:
    - `event_id` (optional)
    - `member_id` (optional) - **「自分のシフト一覧」で必須**
    - `business_day_id` (optional)
    - `slot_id` (optional)
    - `assignment_status` (optional: confirmed/cancelled)
    - `start_date`, `end_date` (optional: 日付範囲フィルタ)
  - *レスポンス*:
    ```json
    {
      "data": {
        "assignments": [
          {
            "assignment_id": "01HXXXX...",
            "slot_id": "01HXXXX...",
            "member_id": "01HXXXX...",
            "member_display_name": "テスト太郎",
            "slot_name": "受付",
            "target_date": "2025-12-05",
            "start_time": "21:30:00",
            "end_time": "23:00:00",
            "assignment_status": "confirmed",
            "assigned_at": "2025-12-03T12:00:00+09:00"
          }
        ],
        "count": 5
      }
    }
    ```
  - *見積もり*: 2〜3時間

- [ ] 🔴 **A-3.3: GET /api/v1/shift-assignments/:assignment_id** [最優先]
  - *レスポンス*: 割り当て情報 + メンバー情報 + シフト枠情報 + 営業日情報
  - *見積もり*: 1時間

---

### 🔴 **親タスク A-4: Member 管理 API の実装**

テスターがメンバー登録・一覧取得できる REST API。

- [x] 🔴 **A-4.1: POST /api/v1/members** [最優先] ✅ **完了 (2025-12-03)**
  - *リクエスト*:
    ```json
    {
      "display_name": "テスト太郎",
      "discord_user_id": "123456789012345678",
      "email": "test@example.com"
    }
    ```
  - *バリデーション*:
    - display_name 必須（1〜50文字）
    - discord_user_id or email のどちらか必須
    - 同一 tenant 内で discord_user_id / email の重複不可
  - *レスポンス*:
    ```json
    {
      "data": {
        "member_id": "01HXXXX...",
        "tenant_id": "01HXXXX...",
        "display_name": "テスト太郎",
        "discord_user_id": "123456789012345678",
        "email": "test@example.com",
        "is_active": true,
        "created_at": "2025-12-03T12:00:00+09:00"
      }
    }
    ```
  - *詳細*: `backend/internal/interface/rest/member_handler.go`
  - *見積もり*: 2時間

- [x] 🔴 **A-4.2: GET /api/v1/members** [最優先] ✅ **完了 (2025-12-03)**
  - *クエリパラメータ*:
    - `is_active` (optional: true/false)
  - *レスポンス*:
    ```json
    {
      "data": {
        "members": [
          {
            "member_id": "01HXXXX...",
            "display_name": "テスト太郎",
            "discord_user_id": "123456789012345678",
            "email": "test@example.com",
            "is_active": true
          }
        ],
        "count": 10
      }
    }
    ```
  - *見積もり*: 1時間

- [ ] 🔴 **A-4.3: GET /api/v1/members/:member_id** [最優先]
  - *レスポンス*: Member 情報のみ（シフト履歴は GET /shift-assignments?member_id=xxx で取得）
  - *見積もり*: 1時間

---

### 🟡 **親タスク A-5: Multi-Tenant / セキュリティ周りの確認**

テナント境界が守られているか、入力バリデーションが統一されているかの確認。

- [ ] 🟡 **A-5.1: テナント境界の徹底確認** [次点]
  - *チェック項目*:
    - [ ] 全 Repository クエリに `tenant_id` を含めているか
    - [ ] REST ハンドラで `X-Tenant-ID`, `X-Member-ID` を context に入れているか
    - [ ] リクエストボディから直接 `tenant_id` を受け取っていないか（ヘッダー優先）
  - *対象ファイル*:
    - `backend/internal/infra/db/*_repository.go`
    - `backend/internal/interface/rest/*_handler.go`
  - *見積もり*: 2時間（レビュー + 修正）

- [ ] 🟡 **A-5.2: 入力バリデーションの整理** [次点]
  - *詳細*: `backend/internal/interface/rest/validator.go`（共通バリデーションヘルパー）
  - *対象*:
    - Event / BusinessDay / ShiftSlot / Assignment / Member のリクエスト struct にタグ付け
    - エラーを `ERR_INVALID_REQUEST` に統一
  - *見積もり*: 2〜3時間

---

## フェーズB: テスター向け Web フロント（α版UI）

### 技術スタック（仮決定）
- **フレームワーク**: React 18 + TypeScript
- **ビルドツール**: Vite（高速・シンプル）
- **HTTP クライアント**: Fetch API（薄いラッパー）
- **スタイリング**: Tailwind CSS（爆速プロトタイピング）
- **状態管理**: useState/useContext（最小限）
- **ルーティング**: React Router v6

**理由**: 最小構成で爆速実装。Next.js は後回し（SSR不要）。

---

### 🔴 **親タスク B-1: フロントエンド基盤のセットアップ**

- [x] 🔴 **B-1.1: Vite + React + TypeScript プロジェクト作成** [最優先] ✅ **完了 (2025-12-03)**
  - *ディレクトリ*: `web-frontend/`（既存の `web/` は API Sandbox として残す）
  - *コマンド*:
    ```bash
    npm create vite@latest web-frontend -- --template react-ts
    cd web-frontend
    npm install
    npm install react-router-dom tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    ```
  - *見積もり*: 30分

- [x] 🔴 **B-1.2: API クライアント層の実装** [最優先] ✅ **完了 (2025-12-03)**
  - *ファイル*: `web-frontend/src/lib/apiClient.ts`
  - *機能*:
    - `baseURL` の設定（環境変数: `VITE_API_BASE_URL`）
    - `X-Tenant-ID`, `X-Member-ID` ヘッダーの自動付与（localStorage から取得）
    - エラーハンドリング（HTTP ステータスコードに応じた throw）
  - *実装例*:
    ```typescript
    export class ApiClient {
      private baseURL: string;
      constructor(baseURL: string) {
        this.baseURL = baseURL;
      }
      private getHeaders(): HeadersInit {
        const tenantId = localStorage.getItem('tenant_id');
        const memberId = localStorage.getItem('member_id');
        return {
          'Content-Type': 'application/json',
          ...(tenantId && { 'X-Tenant-ID': tenantId }),
          ...(memberId && { 'X-Member-ID': memberId }),
        };
      }
      async request<T>(method: string, path: string, body?: any): Promise<T> {
        const res = await fetch(`${this.baseURL}${path}`, {
          method,
          headers: this.getHeaders(),
          body: body ? JSON.stringify(body) : undefined,
        });
        if (!res.ok) {
          const error = await res.json();
          throw new Error(error.error?.message || 'API Error');
        }
        return res.json();
      }
    }
    ```
  - *見積もり*: 1〜2時間

- [x] 🔴 **B-1.3: API 関数の実装** [最優先] ✅ **完了 (2025-12-03)**
  - *ファイル*: `web-frontend/src/lib/api/`
    - `eventApi.ts` - createEvent, getEvents, getEventDetail
    - `businessDayApi.ts` - createBusinessDay, getBusinessDays
    - `shiftSlotApi.ts` - createShiftSlot, getShiftSlots
    - `shiftAssignmentApi.ts` - confirmAssignment, getAssignments
    - `memberApi.ts` - createMember, getMembers
  - *見積もり*: 2〜3時間

---

### 🔴 **親タスク B-2: 画面実装（最小フロー）**

**画面構成**:
1. ログイン/識別画面
2. イベント一覧 + イベント作成
3. 営業日一覧 + 営業日追加
4. シフト枠一覧 + シフト枠追加
5. シフト割当画面
6. 自分のシフト一覧

---

- [x] 🔴 **B-2.1: ① ログイン/識別画面** [最優先] ✅ **完了 (2025-12-03)**
  - *パス*: `/login`
  - *機能*:
    - `display_name` 入力 → `POST /api/v1/members` 呼び出し
    - レスポンスから `member_id` を localStorage に保存
    - `/events` にリダイレクト
  - *UI*:
    - シンプルなフォーム（display_name のみ）
    - 「このα版はテスト用です。データは消える可能性があります」の注意書き
  - *ファイル*: `web-frontend/src/pages/Login.tsx`
  - *見積もり*: 2時間

- [x] 🔴 **B-2.2: ② イベント一覧 + イベント作成画面** [最優先] ✅ **完了 (2025-12-03)**
  - *パス*: `/events`
  - *機能*:
    - `GET /api/v1/events` でイベント一覧を取得
    - 「新しいイベントを作成」ボタン → モーダル or 別ページでフォーム
    - `POST /api/v1/events` でイベント作成
  - *UI*:
    - イベント一覧をカードまたはテーブルで表示
    - 各イベントをクリック → `/events/:event_id` に遷移
  - *ファイル*:
    - `web-frontend/src/pages/EventList.tsx`
    - `web-frontend/src/components/EventCard.tsx`
    - `web-frontend/src/components/CreateEventModal.tsx`
  - *見積もり*: 3〜4時間

- [x] 🔴 **B-2.3: ③ 営業日一覧 + 営業日追加画面** [最優先] ✅ **完了 (2025-12-03)**
  - *パス*: `/events/:event_id/business-days`
  - *機能*:
    - `GET /api/v1/events/:event_id/business-days` で営業日一覧を取得
    - 「営業日を追加」ボタン → フォーム（target_date, start_time, end_time）
    - `POST /api/v1/events/:event_id/business-days` で営業日作成
  - *UI*:
    - 営業日をカレンダービュー or リストで表示
    - 各営業日をクリック → `/business-days/:business_day_id/shift-slots` に遷移
  - *ファイル*:
    - `web-frontend/src/pages/BusinessDayList.tsx`
    - `web-frontend/src/components/CreateBusinessDayModal.tsx`
  - *見積もり*: 3〜4時間

- [x] 🔴 **B-2.4: ④ シフト枠一覧 + シフト枠追加画面** [最優先] ✅ **完了 (2025-12-03)**
  - *パス*: `/business-days/:business_day_id/shift-slots`
  - *機能*:
    - `GET /api/v1/business-days/:business_day_id/shift-slots` でシフト枠一覧を取得
    - 「シフト枠を追加」ボタン → フォーム（slot_name, start_time, end_time, required_count）
    - `POST /api/v1/business-days/:business_day_id/shift-slots` でシフト枠作成
  - *UI*:
    - シフト枠をテーブルで表示（時刻順）
    - `assigned_count / required_count` を表示（例: 1/2）
    - 「割り当て」ボタン → `/shift-slots/:slot_id/assign` に遷移
  - *ファイル*:
    - `web-frontend/src/pages/ShiftSlotList.tsx`
    - `web-frontend/src/components/CreateShiftSlotModal.tsx`
  - *見積もり*: 3〜4時間

- [x] 🔴 **B-2.5: ⑤ シフト割当画面** [最優先] ✅ **完了 (2025-12-03)**
  - *パス*: `/shift-slots/:slot_id/assign`
  - *機能*:
    - `GET /api/v1/members` でメンバー一覧を取得
    - `GET /api/v1/shift-slots/:slot_id` でシフト枠詳細を取得
    - プルダウンでメンバーを選択 → 「確定」ボタン
    - `POST /api/v1/shift-assignments` { slot_id, member_id } で確定
  - *UI*:
    - シフト枠情報（日付・時刻・役職・必要人数）を表示
    - 既に確定済みのメンバー一覧を表示
    - メンバー選択プルダウン + 確定ボタン
    - エラー表示（枠が満員の場合など）
  - *ファイル*: `web-frontend/src/pages/AssignShift.tsx`
  - *見積もり*: 3〜4時間

- [x] 🔴 **B-2.6: ⑥ 自分のシフト一覧画面** [最優先] ✅ **完了 (2025-12-03)**
  - *パス*: `/my-shifts`
  - *機能*:
    - `GET /api/v1/shift-assignments?member_id=<自分のID>` で自分のシフトを取得
    - 日付順・時刻順でソート
  - *UI*:
    - テーブルまたはカードで表示（日付・時刻・役職・イベント名）
    - 「今後のシフト」「過去のシフト」のタブ切り替え
  - *ファイル*: `web-frontend/src/pages/MyShifts.tsx`
  - *見積もり*: 2〜3時間

---

### 🟡 **親タスク B-3: UI/UX 周りの改善**

- [ ] 🟡 **B-3.1: エラー表示の統一** [次点]
  - *機能*: API エラーをトースト or モーダルで表示
  - *ライブラリ*: `react-hot-toast` など
  - *見積もり*: 1時間

- [ ] 🟡 **B-3.2: ローディング表示** [次点]
  - *機能*: API 呼び出し中はスピナーを表示
  - *見積もり*: 1時間

- [ ] 🟡 **B-3.3: フォーム入力バリデーション** [次点]
  - *機能*: 空文字禁止、日付・時刻の形式チェック
  - *見積もり*: 1〜2時間

---

### 🟡 **親タスク B-4: 既存 API Sandbox との整理**

- [ ] 🟡 **B-4.1: API Sandbox を開発者用ツールとして明確化** [次点]
  - *詳細*: `web/index.html` のタイトルを「API Sandbox（開発者用）」に変更
  - *リンク*: フロントエンド UI から API Sandbox へのリンクを追加（デバッグ用）
  - *見積もり*: 30分

---

## フェーズC: テスト環境（インフラ・デプロイ・シード）

### 🔴 **親タスク C-1: ステージング環境の構築**

**ホスティング先の候補**:
- **Render**: 無料枠あり、PostgreSQL 自動プロビジョニング、簡単デプロイ
- **Railway**: 無料枠あり、PostgreSQL 自動プロビジョニング、GitHub 連携
- **Fly.io**: 無料枠あり、Docker ベース、グローバル展開
- **VPS（Hetzner/DigitalOcean）**: 柔軟性高いが手動セットアップ

**推奨**: Render（シンプル・安定・無料枠で十分）

---

- [x] 🔴 **C-1.1: Docker 環境の整備** [最優先] ✅ **完了 (2025-12-03)**
  - *ファイル*:
    - `backend/Dockerfile`（本番用）
    - `web-frontend/Dockerfile`（本番用）
    - `docker-compose.prod.yml`（ステージング用）
  - *見積もり*: 2〜3時間

- [ ] 🔴 **C-1.2: Render へのデプロイ設定** [最優先] 🔄 **準備完了・デプロイ待ち**
  - *手順*:
    1. Render アカウント作成
    2. PostgreSQL データベース作成
    3. Web Service 作成（backend）
      - Docker デプロイ
      - 環境変数: `DATABASE_URL`, `PORT`, `ALLOWED_ORIGINS`
    4. Static Site 作成（web-frontend）
      - Build Command: `npm run build`
      - Publish Directory: `dist`
  - *ドキュメント*: `docs/deployment/render.md`
  - *見積もり*: 2〜3時間

- [ ] 🔴 **C-1.3: 環境変数の整理** [最優先]
  - *Backend*:
    - `DATABASE_URL`
    - `PORT`
    - `ALLOWED_ORIGINS`（CORS用、フロントエンドURLを指定）
    - `LOG_LEVEL`
  - *Frontend*:
    - `VITE_API_BASE_URL`（backend の URL）
    - `VITE_TENANT_ID`（テスト用に固定 tenant_id を埋め込む場合）
  - *見積もり*: 1時間

- [ ] 🔴 **C-1.4: ヘルスチェック監視** [最優先]
  - *機能*: `GET /health` を監視（Render の自動ヘルスチェック or UptimeRobot）
  - *見積もり*: 30分

---

### 🔴 **親タスク C-2: DB シード & リセット**

- [x] 🔴 **C-2.1: テスト用初期データのシードコマンド** [最優先] ✅ **完了 (2025-12-03)**
  - *ファイル*: `backend/cmd/seed/main.go`
  - *シード内容*:
    - Tenant 1件（テスト用）
    - Position 3〜5件（「受付」「案内」「配信」など）
    - Member 0件（ユーザーに作らせる） or 5件（デモ用）
    - Event 1件（デモイベント）
    - BusinessDay 3〜7日分
    - ShiftSlot 1営業日あたり 2〜3枠
  - *実行方法*:
    ```bash
    go run backend/cmd/seed/main.go --env=staging
    ```
  - *見積もり*: 2〜3時間

- [ ] 🟡 **C-2.2: データリセットスクリプト** [次点]
  - *ファイル*: `scripts/reset_db.sh`
  - *機能*:
    1. 全テーブル truncate（外部キー制約を無視）
    2. シード実行
  - *実行方法*:
    ```bash
    ./scripts/reset_db.sh staging
    ```
  - *セキュリティ*: ステージング環境のみ実行可能（本番では禁止）
  - *見積もり*: 1〜2時間

- [ ] 🟡 **C-2.3: 定期リセットの設定（オプション）** [次点]
  - *機能*: 毎晩 3:00（JST）にデータリセット
  - *実装*: Render の Cron Jobs or GitHub Actions（scheduled workflow）
  - *見積もり*: 1時間

---

### 🟢 **親タスク C-3: CI/CD パイプライン**

- [ ] 🟢 **C-3.1: GitHub Actions でステージングへ自動デプロイ** [余裕があれば]
  - *トリガー*: `push to main`
  - *ジョブ*:
    1. Go テスト実行
    2. フロントエンドビルド
    3. Render へデプロイ（Render の Deploy Hook を叩く）
  - *ファイル*: `.github/workflows/deploy-staging.yml`
  - *見積もり*: 2〜3時間

---

## フェーズD: テスター向けドキュメント & 運用

### 🔴 **親タスク D-1: テスターガイドの作成**

- [x] 🔴 **D-1.1: Quick Start ドキュメント** [最優先] ✅ **完了 (2025-12-03)**
  - *ファイル*: `docs/tester/quickstart.md`
  - *内容*:
    1. はじめに（これは α 版、データは消える可能性あり）
    2. アクセス URL（ステージング環境）
    3. 最初にやること
      - ログイン（display_name 入力）
      - イベント一覧を見る
    4. 操作フロー
      - イベント作成
      - 営業日作成
      - シフト枠作成
      - メンバー登録（別のユーザーとして）
      - シフト割り当て
      - 自分のシフト確認
    5. バグ報告方法（Discord チャンネル or GitHub Issues へのリンク）
  - *スクリーンショット*: 各画面の操作例を添付
  - *見積もり*: 2〜3時間

- [x] 🔴 **D-1.2: テストシナリオ（お願いしたい動作確認）** [最優先] ✅ **完了 (2025-12-03)**
  - *ファイル*: `docs/tester/test-scenarios.md`
  - *内容*:
    - **シナリオA**: 管理者がイベントを作ってシフトを埋める（正常系）
    - **シナリオB**: 複数人で同じ枠に同時に入って競合させる（エラー確認）
    - **シナリオC**: 深夜枠（21:00–02:00）を作って UI 表示が崩れないか
    - **シナリオD**: テナント境界の確認（別テナントのデータが見えないか）
  - *見積もり*: 1〜2時間

---

### 🟡 **親タスク D-2: バグ報告・フィードバック**

- [ ] 🟡 **D-2.1: バグ報告チャネルの設定** [次点]
  - *選択肢*:
    - Discord の専用チャンネル（`#alpha-test-bugs`）
    - GitHub Issues テンプレート
  - *テンプレート例*:
    ```markdown
    ## バグ報告
    - 発生日時:
    - 操作内容:
    - 期待する動作:
    - 実際の動作:
    - スクリーンショット（あれば）:
    ```
  - *見積もり*: 1時間

- [ ] 🟡 **D-2.2: ログ/監視の整備** [次点]
  - *機能*:
    - サーバーログに request-id を付ける（トレースしやすく）
    - パニック時のスタックトレースがちゃんと出るように確認
  - *実装*: ミドルウェアで request-id 生成 + context に埋め込み
  - *見積もり*: 1〜2時間

---

## フェーズE: Nice-to-have（テスト後 or 並行でもOK）

### 🟢 **親タスク E-1: Discord Bot（UI代替 or 補完）**

- [ ] 🟢 **E-1.1: Backend API クライアントの実装** [余裕があれば]
  - *ファイル*: `bot/src/services/backendClient.ts`
  - *機能*: Event / ShiftSlot / ShiftAssignment / Member API の薄いラッパー
  - *見積もり*: 2時間

- [ ] 🟢 **E-1.2: 基本コマンドの実装** [余裕があれば]
  - `/event create` - イベント作成
  - `/shift create-slot` - シフト枠作成
  - `/shift confirm` - シフト確定
  - `/shift my-shifts` - 自分のシフト一覧
  - *見積もり*: 4〜6時間

---

### 🟢 **親タスク E-2: Notification / Audit のちゃんとした実装**

- [ ] 🟢 **E-2.1: Discord 通知の actual 送信** [余裕があれば]
  - *詳細*: NotificationSender インターフェースの実装
  - *見積もり*: 2〜3時間

- [ ] 🟢 **E-2.2: 通知履歴・監査ログの閲覧API** [余裕があれば]
  - `GET /api/v1/notifications/logs`
  - `GET /api/v1/audit/logs`
  - *見積もり*: 2〜3時間

---

## 🎯 ざっくり優先度まとめ

### 🔴 **最優先（まずここまでやれば「一般人テスト」は回せる）**

**バックエンド**:
- A-1: ShiftAssignment Application Service
- A-2: ShiftSlot 管理 API
- A-3: ShiftAssignment 管理 API
- A-4: Member 管理 API

**フロントエンド**:
- B-1: フロントエンド基盤のセットアップ
- B-2: 画面実装（6画面）

**インフラ**:
- C-1: ステージング環境の構築
- C-2.1: シードコマンド

**ドキュメント**:
- D-1: テスターガイドの作成

**見積もり合計**: **約 60〜80 時間**（1人で集中すれば 2週間、並行作業なら 1週間）

---

### 🟡 **次点（テストしながら整える）**

- A-5: Multi-Tenant / セキュリティ周りの確認
- B-3: UI/UX 周りの改善
- C-2.2: データリセットスクリプト
- D-2: バグ報告・フィードバック

**見積もり合計**: **約 10〜15 時間**

---

### 🟢 **余裕があれば（テスト後でもOK）**

- C-3: CI/CD パイプライン
- E-1: Discord Bot
- E-2: Notification / Audit のちゃんとした実装

**見積もり合計**: **約 15〜20 時間**

---

## 📋 実装時の注意事項

### 1. Multi-Tenant 前提設計
- 全 API ハンドラで `X-Tenant-ID` ヘッダーを必須とする
- フロントエンドは localStorage に `tenant_id` を保存し、全リクエストで送信
- テスト用には「招待URL に tenant_id を埋め込み」で固定テナントを使わせる

### 2. DDD レイヤ保護ルール
- **状態変更系 API（POST/PUT/PATCH/DELETE）は必ず Application Service 経由**
- ハンドラから直接 Repository を呼び出して永続化してはいけない
- **参照系 API（GET）のみ**、Repository 直接アクセスを許可

### 3. フロントエンドの割り切り
- **α 版なので最小限の UI**（デザインは後回し）
- **Tailwind CSS でサクッと作る**（カスタムCSS は書かない）
- **状態管理は useState/useContext で十分**（Redux は過剰）

### 4. インフラの割り切り
- **ステージング環境のみ**（本番環境は後回し）
- **Render の無料枠で十分**（スケールは後回し）
- **データリセットは手動 or Cron**（自動リセットは後回し）

---

## 🚀 次のステップ

1. 🔴 **A-1 〜 A-4: バックエンド API を完成させる**（最優先）
2. 🔴 **B-1 〜 B-2: フロントエンド最小実装**（並行作業可能）
3. 🔴 **C-1 〜 C-2.1: ステージング環境構築 + シード**
4. 🔴 **D-1: Quick Start ドキュメント**
5. 🎉 **テスター招待 & フィードバック収集**

---

## 完了したタスク

### 2025-12-03: Go アップグレード + バックエンド API + フロントエンド UI 完成 🎉

**環境整備**:
- ✅ Go 1.24.11 へのアップグレード（恒久化完了）
- ✅ ブートストラップスクリプト作成（`scripts/bootstrap.sh`, `scripts/install-go-local.sh`）
- ✅ SETUP.md, README.md 作成

**バックエンド API（フェーズA）**:
- ✅ A-1.1: ShiftAssignmentService 実装（同時確定制御 + Notification/AuditLog stub）
- ✅ A-2.1, A-2.2, A-2.3: ShiftSlot 管理 API（POST/GET）
- ✅ A-3.1, A-3.2: ShiftAssignment 管理 API（POST/GET + Service 経由）
- ✅ A-4.1, A-4.2: Member 管理 API（POST/GET）

**フロントエンド UI（フェーズB）**:
- ✅ B-1.1, B-1.2, B-1.3: Vite + React + TypeScript 基盤 + API クライアント
- ✅ B-2.1: ログイン/識別画面
- ✅ B-2.2: イベント一覧 + 作成画面
- ✅ B-2.3: 営業日一覧 + 追加画面
- ✅ B-2.4: シフト枠一覧 + 追加画面
- ✅ B-2.5: シフト割当画面
- ✅ B-2.6: 自分のシフト一覧画面

**次のステップ**: フェーズC（インフラ）とフェーズD（ドキュメント）

### 2025-12-03: インフラ整備 + ドキュメント完成 🎉

**インフラ（フェーズC）**:
- ✅ C-1.1: Docker 環境整備（backend/Dockerfile, web-frontend/Dockerfile）
- ✅ C-1.2: 環境変数の整理とドキュメント化（docs/ENVIRONMENT_VARIABLES.md）
- ✅ C-2.1: シードコマンド実装（cmd/seed/main.go）
- ✅ データベースリセットスクリプト（scripts/reset-db.sh）
- ✅ デプロイ前チェックスクリプト（scripts/deploy-check.sh）
- ✅ docker-compose.prod.yml（ステージング/本番用）

**ドキュメント（フェーズD）**:
- ✅ D-1.1: Quick Start ドキュメント（docs/tester/quickstart.md）
- ✅ D-1.2: テストシナリオ（docs/tester/test-scenarios.md）
- ✅ SETUP.md（開発環境セットアップ）
- ✅ README.md（プロジェクト概要）

**現状**: 
- ✅ すべてのコード実装完了
- ✅ ローカル環境での動作確認可能
- 🔄 クラウドデプロイは未実施（Docker環境はすべて準備完了）

**次のステップ**: 
1. ローカルでの動作確認
2. Render または Railway へのデプロイ（C-1.2）
3. Public Alpha テスト開始

---

## ブロック中 / 要注意

*進行中に発生したブロッカーをここに記録する*

---

## 関連ファイル

### 既存のタスクファイル
- `backend/TASKS_STATIC_DOMAIN_AND_NOTIFICATION_FLOW.md` - ドメイン・API の詳細設計

### ドキュメント
- `docs/tester/quickstart.md` - テスター向け Quick Start（これから作成）
- `docs/tester/test-scenarios.md` - テストシナリオ（これから作成）
- `docs/deployment/render.md` - Render デプロイ手順（これから作成）

### フロントエンド
- `web-frontend/` - テスター向け Web UI（これから作成）
- `web/` - API Sandbox（開発者用、既存）

### Backend
- `backend/cmd/seed/main.go` - シードコマンド（これから作成）
- `backend/internal/app/shift_assignment_service.go` - ShiftAssignment Application Service（これから作成）
- `backend/internal/interface/rest/shift_slot_handler.go` - ShiftSlot REST API（これから作成）
- `backend/internal/interface/rest/shift_assignment_handler.go` - ShiftAssignment REST API（これから作成）
- `backend/internal/interface/rest/member_handler.go` - Member REST API（これから作成）

---

## 改善メモ

*進行中に気づいた改善点をここに記録する*

