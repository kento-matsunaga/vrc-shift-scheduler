# VRC Shift Scheduler - Production Environment Variables
# Copy this file to .env.prod and fill in the values
# NEVER commit .env.prod to git!

# ==================== Database (Required) ====================
POSTGRES_DB=vrcshift
POSTGRES_USER=vrcshift
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ==================== Authentication (Required) ====================
# Generate with: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=CHANGE_ME_64_CHAR_RANDOM_STRING

# ==================== URLs (Required) ====================
# Frontend URL for API access (include protocol, no trailing slash)
VITE_API_BASE_URL=https://api.your-domain.com

# ==================== Ports (Optional) ====================
# Defaults: backend=8080, frontend=80
# BACKEND_PORT=8080
# FRONTEND_PORT=80

# ==================== Backend Configuration (Optional) ====================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# CORS: Comma-separated list of allowed origins
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ==================== Feature Flags (Optional) ====================
ENABLE_AUDIT_LOG=false
ENABLE_NOTIFICATION=false

# ==================== Email (AWS SES) (Optional) ====================
# If not set, emails are logged instead of sent (development mode)
# AWS_REGION=ap-northeast-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_SES_FROM_EMAIL=noreply@your-domain.com

# Base URL for invitation links (used in email templates)
# INVITATION_BASE_URL=https://your-domain.com

# ==================== Discord Bot (Optional) ====================
# Only required if using: docker compose --profile bot up
# DISCORD_BOT_TOKEN=your_bot_token
# DISCORD_APP_ID=your_app_id
